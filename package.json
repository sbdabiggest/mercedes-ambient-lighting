{
  "name": "lumen-mercedes-ambient-lighting",
  "version": "1.0.0",
  "private": true,
  "description": "LUMEN — Mercedes ambient lighting install landing page (single-file static site).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
